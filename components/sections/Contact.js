import SectionTitle from "../SectionTitle";

export default function Contact({ section }) {
  return (
    <div className="flex min-h-screen flex-col justify-evenly pt-16">
      <SectionTitle section={section} />
    </div>
  );
}
